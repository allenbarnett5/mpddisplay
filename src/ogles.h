/*
 * A few support routines for OpenGL ES. Which is very wordy.
 */
#pragma once

GLuint OGLEScreateProgram ( const char* vertex_shader_src,
                            const char* fragmt_shader_src );
